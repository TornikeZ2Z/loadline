node_modules/
.next/
.env
.env.local
.pgdata/
# Per-agent isolated databases (.pgdata-agent-a, .pgdata-agent-b, ...)
.pgdata-*/
*.tsbuildinfo
next-env.d.ts
.claude/

# Design-pass artifacts (large, partly superseded drafts); durable but not published
.design/

# Dev-server log from the design pass
.devlog.txt

# OpenTofu — state and provider caches never go in git.
# infra/*.tfvars IS committed: it holds no secrets.
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.*
infra/*.tfplan
infra/.terraform.lock.hcl.bak
